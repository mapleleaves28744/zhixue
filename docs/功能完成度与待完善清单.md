# 智学工坊功能完成度与待完善清单

> 文档状态：**当前实现事实源**
> 更新日期：2026-06-06

## 当前结论

学生端主链路已经实际跑通，前端 Sprint 1-4 已完成，真实 LLM 主链路已通过。当前工作重点不再是补齐基础闭环，而是增强展示深度、自动化验收、演示数据和部署可靠性。

## 已完成

| 模块 | 当前状态 | 证据 |
|---|---|---|
| Auth / Course | 注册登录、课程 CRUD 可用 | API、Stitch 页面、真实主链路 |
| Material / RAG | 上传、解析、切片、向量化、抽取、检索可用 | `/knowledge`、真实主链路 |
| LLM Wiki | 生成、列表、详情、来源、版本、编辑/回滚 API 可用 | `/knowledge`、API |
| Tutor / Resource | 真实模型问答、引用、反馈、保存、资源生成可用 | `/assistant`、真实主链路 |
| Quiz / Diagnosis | 练习、提交、错题、历史诊断、推荐可用 | `/practice`、真实主链路 |
| Profile / Memory | 画像查看、重建、记忆查看/反思/删除可用 | `/path-profile` |
| Evolution / Path | 策略生成、应用、回滚、路径生成与完成项可用 | `/path-profile` |
| Agent | 13 个任务型 Agent，运行日志可查询 | `agent_runs`、API |
| 前端视觉 | 共享样式、Framer 壳层、本地品牌/IP 资产、关键空状态完成 | Sprint 1-4 记录 |
| 安全升级 | Next.js 16.2.7，npm audit 归零 | 构建与审计记录 |

## 当前主链路

```text
注册登录 → 创建课程 → 上传资料 → 解析 → 切片 → 向量化
→ 知识点抽取 → RAG → Wiki → Tutor → 资源
→ 练习 → 错题 → 诊断 → 画像/记忆 → 自进化 → 推荐 → Agent 日志
```

真实验收使用 `xiaomi_mimo / mimo-v2.5`，`fallback_used=false`。详见 `19_测试方案/13_真实LLM主链路与Next安全专项验收记录.md`。

## 页面状态

| 页面 | 当前可演示能力 | 后续增强 |
|---|---|---|
| `/` | 品牌首页、登录、注册、本地品牌资产 | 导航锚点与品牌内容细化 |
| `/home` | 资料、Wiki、诊断、Agent 活动摘要 | 快捷提问与趋势增强 |
| `/courses` | 课程列表、创建、编辑 | 搜索与课程内聚合 |
| `/knowledge` | 资料流水线、RAG、Wiki 详情/来源/版本 | 图谱视觉、Wiki 编辑 UI |
| `/assistant` | Tutor、引用、资源、反馈、保存 Wiki | 独立聊天历史 |
| `/practice` | 生成练习、提交、错题、历史诊断 | 掌握度可视化、推荐反馈 |
| `/dashboard` | 学习概览、推荐、Agent 统计 | 真实趋势与任务状态 |
| `/path-profile` | 画像、记忆、路径、自进化应用/回滚 | Agent run 详情、策略 diff |

## 当前明确缺口

### P0：比赛演示可靠性

1. 一键初始化《数据结构》完整演示数据。
2. 浏览器级自动化 E2E，覆盖登录到自进化。
3. Docker Compose 全栈启动验收。
4. 更稳定的真实模型超时、重试和成本记录。

### P1：创新展示深度

1. Wiki 图谱可交互展示。
2. Agent 调用链详情与 Review 结果展示。
3. 自进化策略前后差异、证据与风险确认强化。
4. 学习趋势、掌握度、推荐反馈真实化。

### P2：工程增强

1. Redis 缓存和任务状态实际接入。
2. 大文件异步解析与进度。
3. 独立聊天会话/历史。
4. MinIO、Rerank、GraphRAG 等增强能力。

## 范围冻结

- 教师端：不建设。
- 管理员后台：当前无页面、无 `/api/v1/admin/*`，不应宣称已实现。
- 旧 React `/student/*` 页面：不建设。

## 当前数字

| 指标 | 当前值 |
|---|---:|
| API HTTP 操作 | 89 |
| ORM 表 | 28 |
| migration | 16 |
| pytest | 91 passed |
| Next 页面 | 10 |
| Stitch 页面 | 8 |
| npm audit | 0 vulnerabilities |

API 与数据库精确清单见：

- `docs/11_API接口设计/16_当前实现API清单.md`
- `docs/10_数据库设计/15_当前实现数据库清单.md`
