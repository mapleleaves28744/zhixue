# 02_版本变更记录

> 用途：记录 `docs/` 的重要版本变更。实现事实优先参考
> [`当前实现基线.md`](../当前实现基线.md)。

## 2026-07-13：`change_8` 活动文档与录制材料对齐

- 当前实现基线更新为 `change_8`：147 个 HTTP 操作、44 张 ORM 表、24 个 migration、69 个 Service 文件、61 个后端测试文件、466 项 pytest 通过。
- 补充 Agent Runtime 候选工具边界、非候选工具重规划、工具失败回滚、原子认领和取消竞态收敛事实。
- 明确真实 Provider 全量验收尚未建立，不用 2026-06-06 的 23 步真实 LLM 主链路替代全量媒体矩阵。
- 更新 README、文档索引、比赛提交总览、证据索引、PPT/答辩数字和代码—文档差异表；历史验收记录保留原日期与原结果。
- 将演示视频脚本补成约七分钟的逐段画面、操作和完整旁白底稿。
- 在功能完成度清单加入“现有功能优化建议”，只覆盖安全、速度、稳定性、维护性和现有体验，不扩展教师端、管理员端、PWA、MinIO、完整 GraphRAG 或数字人等新能力。
- 本轮没有修改 Router、Schema、SQLAlchemy Model、数据库迁移或前端页面。

## 2026-06-08：基础设施与 AI 智能升级

本次为 change_3 分支落地二次升级方案中的基础设施增强和 AI 智能升级部分。

### 新增文件

- `backend/app/llm/adapters/base.py` — `structured_chat()` 结构化输出方法
- `backend/app/core/event_bus.py` — 发布-订阅事件总线
- `backend/app/core/event_handlers.py` — 3 个系统事件处理器
- `backend/app/services/difficulty_service.py` — 自适应难度调节服务
- `backend/app/services/generator_registry.py` — 生成器插件注册表
- `backend/app/generators/` — mindmap/flashcard/podcast_script 3 个生成器插件
- `backend/app/models/ab_test.py` — A/B 测试模型
- `backend/app/services/ab_test_service.py` — A/B 测试服务
- `backend/app/api/v1/ab_tests.py` — A/B 测试 API（7 端点）
- `backend/app/db/pgvector_setup.py` — pgvector HNSW 索引自动创建
- `frontend/app/evolution/page.tsx` — 自进化策略仪表盘页面
- `frontend/services/evolutionService.ts` — Evolution API 服务
- `frontend/components/reactor/` — Reactor 流式渲染组件

### 修改文件

- `backend/app/core/config.py` — 新增 7 个配置项（难度阈值、自进化触发等）
- `backend/app/agents/planner_agent.py` — 多目标优化路径规划
- `backend/app/agents/resource_agent.py` — 插件生成器集成
- `backend/app/services/diagnosis_service.py` — 难度调节 + 事件发布
- `backend/app/services/quiz_service.py` — 自适应难度 + 事件发布
- `backend/app/services/evolution_service.py` — 使用配置替代硬编码阈值
- `backend/app/main.py` — EventBus 生命周期 + pgvector 启动设置
- `backend/app/api/v1/router.py` — 注册 ab_tests 路由
- `backend/app/api/v1/diagnosis.py` — 新增 difficulty 端点

### 更新文档

- `docs/_archive/二次升级方案.md` — 追加 §29 升级落地记录
- `docs/功能完成度与待完善清单.md` — 新增 12 项已完成 + 3 项待完成
- `docs/当前实现基线.md` — 更新规模数字、前端页面、后端能力描述
- `docs/当前实现API清单.md` — 新增 8 个 API 端点
- `docs/当前实现数据库清单.md` — 新增 ab_tests、ab_test_assignments 表

---

## 2026-06-06：当前实现事实源校准

本次校准解决旧设计方案、规划文档与实际代码不一致时容易误导后续开发的问题。

### 新增

- 新增 `docs/当前实现基线.md`，集中记录当前技术栈、页面形态、主链路和明确缺口。
- 新增 `docs/00_文档规范/04_文档事实源与状态规则.md`，定义实现事实、设计约束和规划参考的优先级。
- 新增 `scripts/export_implementation_docs.py`，从 FastAPI OpenAPI 与 SQLAlchemy metadata 自动导出当前接口和数据库清单。
- 新增当前 OpenAPI 快照、89 个 HTTP 操作清单和 28 张 ORM 表清单。

### 更新

- 更新 `AGENTS.md`、根目录 `README.md` 与文档索引，要求后续开发先核对当前实现事实源。
- API 文档按当前 FastAPI Router 重新生成；未实现的管理员后台、Token 刷新和退出接口不再描述为已完成。
- 前端页面与路由文档更新为当前 Next.js App Router + `StitchFrame` + Stitch 静态页架构。
- 更新系统架构、功能完成度、PRD、Wiki、Agent、自进化、RAG 等文档的状态说明，避免将目标蓝图误认为当前实现。

### 维护约定

Router、Schema 或 SQLAlchemy Model 变化后执行：

```powershell
python scripts/export_implementation_docs.py
```

生成文件应与对应代码变更一并提交。

## 2026-06-06：编号目录文档整合

- 将 `docs/00_文档规范` 至 `docs/24_答辩稿` 的占位主题合并为目录同名总文档。
- 删除 232 份空白、占位或已被总文档吸收的 Markdown 文件。
- 保留 API/数据库当前清单、阶段验收、真实 LLM 验收、本地启动指南等独立价值文档。
- 所有编号目录总文档均增加“目录导读”，并区分当前实现、目标设计和冻结范围。
- 新增 `scripts/check_docs.py`，检查目录入口、占位模板和本地 Markdown 断链。
- 完整核验结果见 `docs/_archive/文档整理核验报告.md`（2026-06-06 历史记录，数字已过时；当前以 `当前实现基线.md` 为准）。
