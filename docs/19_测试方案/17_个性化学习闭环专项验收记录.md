# 个性化学习闭环专项验收记录

验收日期：2026-06-13

## 验收范围

本次验收覆盖长期记忆、自进化策略物化、双层学习画像与主页真实学习分析。

## 数据库与迁移

- 执行 `python -m alembic upgrade head` 成功。
- 当前数据库迁移版本：`c5d6e7f8a9b0 (head)`。
- 历史长期记忆由 `90 active / 50 archived` 整理为 `63 active / 77 archived`。
- 单个用户课程作用域最大活跃记忆数为 `20`。
- 历史重复和容量超限记忆均归档，未删除。

## 自动化测试

- `python -m pytest -q`：`287 passed`。
- `python -m pytest tests/test_personalization_closed_loop.py -q`：`4 passed`。
- `npm run typecheck`：通过。
- `npm run build`：通过。
- `node scripts/personalization-closed-loop.test.mjs`：通过。
- `node scripts/pet-companion.test.mjs`：通过。
- `node scripts/assistant-send-stop-button.test.mjs`：通过。
- `python scripts/export_implementation_docs.py`：成功导出当前 API 与数据库清单。
- `python scripts/check_docs.py`：通过。

## 浏览器验收

在 `http://localhost:3000` 使用当前登录态完成检查：

1. `/home` 学习分析不再展示静态 `18.5 小时 / 82%`，显示真实 API 返回的本周数据。
2. `/home` 可见本周/本月选择器、真实学习时长、真实掌握度与按日时长柱状图。
3. `/path-profile` 显示全局画像与当前课程画像。
4. `/path-profile` 不再使用默认 `68%` 掌握度。
5. `/path-profile` 显示活跃/归档/全部记忆切换与 `20/20` 容量提示。
6. 活跃记忆展示强化次数，归档记忆支持恢复。

## 已知限制

- 策略效果评估当前在达到 5 个相关行为或运行 7 天后生成待人工复核摘要，尚未完成完整因果效果统计。
- Stitch iframe 中非必要的动态 `MutationObserver` 滚动显现监听已关闭；保留首次滚动显现，页面重新加载后不再产生该运行时错误。
- 本次使用当前本地 Provider 配置完成回归，没有执行真实 LLM 成本与效果专项。
