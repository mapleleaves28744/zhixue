# 19_测试方案

## 目录导读

> 文档状态：**当前测试与验收入口**
>
> 当前实现事实优先参考：`docs/当前实现基线.md`、当前代码、OpenAPI、SQLAlchemy Model 与验收记录。

本文合并了本目录原有占位主题。阅读顺序：

- [当前测试策略](#当前测试策略)
- [测试分层](#测试分层)
- [阶段验收](#阶段验收)
- [真实LLM专项](#真实llm专项)
- [全量验收矩阵](#全量验收矩阵)

当前活动回归快照（2026-07-13，`change_8`）：`cd backend && .venv/bin/python -m pytest tests -q`，结果为 **466 passed，6 warnings**。API 与数据库清单复核结果为 **147 个 HTTP 操作、44 张 ORM 表**。历史验收记录中的较小测试数保留当时日期，不作为当前规模口径。

## 当前测试策略


项目当前采用“本地优先，Docker 后置”的测试策略：

1. 日常开发优先验证本地 Python、Node、本机 PostgreSQL、本机 Redis 环境；
2. 后端任务运行 `python -m pytest` 或相关测试；
3. 前端任务运行 `npm run typecheck` 和 `npm run build`；
4. 数据库任务运行 `python -m alembic upgrade head`；
5. 涉及 LLM 的任务必须保证 Mock Provider 可运行；
6. Docker 只在第21阶段或部署专项阶段作为强制验收。

阶段级验收详见：

```text
docs/19_测试方案/12_阶段运行验收清单.md
```

基础本地检查脚本：

```powershell
scripts/local_check.ps1 -All
```

真实 LLM 主链路专项验收：

```powershell
# 先启动已配置真实 LLM Provider 的后端
scripts/local_check.ps1 -MainChain
```

`-MainChain` 会执行注册、建课、资料处理、Wiki、Tutor、资源、练习、诊断、画像、记忆、自进化、推荐与 Agent 日志检查。若 Tutor 回退到 Mock，验收直接失败。最近一次专项结果见 `13_真实LLM主链路与Next安全专项验收记录.md`。

Phase 1《数据结构》公有知识库阶段验收见：

```text
docs/19_测试方案/14_Phase1数据结构知识库阶段验收记录.md
```

Phase 2/3 对话式 AgentTask 与规划执行器阶段验收见：

```text
docs/19_测试方案/15_Phase2与Phase3Agent任务执行器阶段验收记录.md
```

Phase 3.1 LangGraph + MiMo 真正学习智能体阶段验收见：

```text
docs/19_测试方案/16_Phase3.1LangGraph真正智能体阶段验收记录.md
```

Phase 4 对话式画像与证据化学生模型阶段验收见：

```text
docs/19_测试方案/17_Phase4对话式画像阶段验收记录.md
```

个性化学习闭环（记忆合并、策略物化、双层画像、学习分析）专项验收见：

```text
docs/19_测试方案/17_个性化学习闭环专项验收记录.md
```

OpenMAIC 个性化沉浸课堂与配音字幕 MP4 阶段验收见：

```text
docs/19_测试方案/18_OpenMAIC沉浸课堂阶段验收记录.md
```

Grounded QA 快速/智能体双通道、引用可靠性与响应式助手专项验收见：

```text
docs/19_测试方案/23_Grounded_QA双通道验收记录.md
```

`change_6` 与 `change_7` 合并、冲突取舍和合并后全量验证见：

```text
docs/19_测试方案/24_change6与change7合并验收记录.md
```

真实 Provider 全量验收的当前准备度与真实性边界见：

```text
docs/19_测试方案/25_真实Provider全量验收记录.md
```

该记录目前明确为“全量基线尚未建立”；2026-06-06 的 23 步真实 LLM 主链路不能替代文本、图像、音频、视频与沉浸课堂的全量矩阵。

智能对话资源生成工具专项验收见：

```text
docs/19_测试方案/22_智能对话资源生成工具专项验收记录.md
```

全量功能扫描与浏览器验收、缺陷闭环见：

```text
docs/19_测试方案/20_全量功能扫描与浏览器验收报告.md
docs/19_测试方案/21_缺陷清单与修复闭环记录.md
```

## 测试分层

- 单元/服务测试：后端 `python -m pytest`。
- 数据库：`python -m alembic upgrade head`。
- 前端：`npm run typecheck`、`npm run build` 和关键页面浏览器烟测。
- 主链路：`python scripts/main_chain_check.py`，真实验收禁止 Mock 回退。
- 安全：`npm audit --audit-level=moderate` 和权限隔离测试。
- Agent 评测：`python scripts/evaluate_agent_runtime.py --validate-only` 校验 20 条场景；真实批量评测需传 `--user-id`、`--course-id`。
- Agent 稳定演示：`python scripts/agent_demo_check.py --base-url http://127.0.0.1:8000/api/v1` 校验 `/assistant`、工具事件、画像证据和学习路径。
- OpenMAIC：`scripts/local_check.ps1 -OpenMAIC` 校验内部鉴权和生产构建；后端聚焦测试覆盖客户端、个性化上下文、Agent 路由与真实 MP4 文件导出。

## 阶段验收

阶段完成必须对照 `12_阶段运行验收清单.md`，未通过时只能说明实现完成但验收未通过。

## 真实LLM专项

最近一次真实 LLM 与 Next 安全升级证据保存在 `13_真实LLM主链路与Next安全专项验收记录.md`。

## 全量验收矩阵

统一矩阵字段为：功能模块、页面入口、相关 API、前置数据、操作步骤、预期结果、权限要求、异常场景、自动化覆盖、浏览器覆盖、证据位置和验收状态。

| 类别 | 必测内容 | 主要证据 |
|---|---|---|
| 主链路 | Auth、课程、资料、RAG/Wiki、Tutor、资源、练习、诊断、画像、记忆、自进化、推荐 | MainChain、AgentDemo、浏览器 |
| 权限 | 未登录、跨用户、课程归属、策略详情、媒体资产、A/B 统计 | pytest、浏览器攻击链 |
| 异常 | 无课程/资料、LLM 降级、Worker/OpenMAIC 不可用、SSE 中断、任务取消 | pytest、专项脚本 |
| 工程 | migration、构建、文档、OpenMAIC 生产构建 | `local_check.ps1`、`check_docs.py` |
| 智能对话资源 | 资源侧栏、13 种直接资源类型、图片/视频/课件/沉浸课堂/TTS Agent 工具 | 资源工具专项矩阵、浏览器 |

本轮真实结果见 `20_全量功能扫描与浏览器验收报告.md`，缺陷闭环见 `21_缺陷清单与修复闭环记录.md`。
