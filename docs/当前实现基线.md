# 智学工坊当前实现基线

> 文档状态：**当前实现事实源**
>
> 基线日期：2026-06-06
>
> 分支：`change_2`
>
> 用途：回答“项目现在实际长什么样、实际能做什么”。

## 一句话结论

当前项目是一个本地优先运行的学生端学习闭环：Next.js 16 使用 `StitchFrame` 承载 8 个静态视觉页面，静态页面通过共享 JavaScript 调用 FastAPI；后端为模块化单体，使用 PostgreSQL/pgvector、统一 LLM Provider 和轻量多 Agent 编排。

## 当前规模

| 项目 | 当前事实 |
|---|---:|
| FastAPI HTTP 操作 | 90 |
| SQLAlchemy ORM 表 | 28 |
| Alembic migration | 17 |
| Agent 类 | 13（含 Orchestrator 与 Review） |
| Next.js 页面路由 | 10 |
| Stitch HTML 页面 | 8 |
| 后端 pytest | 108 passed |
| 真实 LLM 主链路 | 23 步通过 |
| 前端依赖审计 | 0 vulnerabilities |

## 当前前端形态

| 路由 | 实际承载 |
|---|---|
| `/` | `brand-home.html`，品牌首页与登录/注册弹窗 |
| `/home` | `home.html`，登录后学习首页 |
| `/courses` | `courses.html`，课程创建、编辑、归档入口 |
| `/knowledge` | `knowledge.html`，资料处理、RAG、Wiki 列表与详情 |
| `/assistant` | `assistant.html`，Tutor 与资源生成 |
| `/practice` | `practice.html`，练习、错题、历史诊断 |
| `/dashboard` | `dashboard.html`，学习概览与推荐 |
| `/path-profile` | `path-profile.html`，画像、记忆、路径、自进化 |
| `/login` | 重定向到 `/?auth=login` |
| `/register` | 重定向到 `/?auth=register` |

当前不是 React 业务组件化应用。Next.js 负责路由和 iframe 壳层，主要业务交互位于 `frontend/public/stitch-pages/*.html` 与 `zhixue-static-api.js`。当前没有 middleware 路由鉴权，页面通过 Token 和 API 401 响应处理登录状态。

## 当前后端形态

- FastAPI 模块化单体：Router → Service → Repository → Model。
- 业务 API 位于 `/api/v1`，当前无 `/api/v1/admin/*`。
- PostgreSQL 是业务主存储，pgvector 用于向量字段和检索。
- 公有《数据结构》课程知识库已可初始化为 `public_template`，普通用户可只读访问；当前本地验收中已入库 32 份资料、1608 个 chunks、1608 个真实 BGE embedding、125 个知识点。
- RAG 检索已从单纯向量检索增强为向量检索 + 关键词检索 + metadata 过滤 + 轻量 rerank/source diversity 的 HybridRetriever。
- Redis URL 已配置，但当前业务代码尚未实际使用 Redis。
- 上传文件使用本地存储 `backend/storage`；当前无 MinIO Adapter。
- LLM 支持 Mock 和 OpenAI-compatible Adapter；真实验收使用 `xiaomi_mimo / mimo-v2.5`。
- OpenAI-compatible 调用失败时可回退 Mock；真实验收要求 `fallback_used=false`。
- Embedding Provider 与聊天 Provider 分开配置；真实聊天模型通过不等于真实 Embedding 已启用。

## 当前 Agent

已实现：

`OrchestratorAgent`、`ProfileAgent`、`MemoryAgent`、`WikiAgent`、`KnowledgeAgent`、`PlannerAgent`、`ResourceAgent`、`QuizAgent`、`TutorAgent`、`DiagnosisAgent`、`RecommendAgent`、`EvolutionAgent`、`ReviewAgent`。

Agent 使用统一 `AgentContext` / `AgentResult`，运行记录写入 `agent_runs`。当前属于任务型工作流编排，不是开放式自治 Agent。

## 当前已验收主链路

```text
注册登录 → 创建课程 → 上传资料 → 解析 → 切片 → 向量化
→ 知识点抽取 → RAG 检索 → Wiki 生成/详情/版本
→ Tutor → 资源 → 练习 → 错题 → 诊断
→ 画像 → 记忆反思 → 自进化 → 推荐 → Agent 日志
```

真实验收详情见 `docs/19_测试方案/13_真实LLM主链路与Next安全专项验收记录.md`。

## 当前明确未实现或未完成

- 教师端页面和业务接口。
- 管理员后台页面和 `/api/v1/admin/*`。
- 独立聊天会话/历史列表。
- WebSocket、Celery 等后台任务系统。
- Redis 业务缓存和任务状态。
- MinIO 存储。
- 完整 Microsoft GraphRAG community summaries / Global-Local-DRIFT 查询。
- Cross-encoder 或 LLM rerank 等高级重排序。
- Docker 全栈正式验收。
- 完整演示种子数据一键初始化。
- 全站端到端浏览器自动化测试。

## 事实源

| 判断内容 | 优先读取 |
|---|---|
| API 是否存在 | `docs/11_API接口设计/16_当前实现API清单.md`、OpenAPI、Router |
| 数据表与字段 | `docs/10_数据库设计/15_当前实现数据库清单.md`、migration、Model |
| 页面与交互 | `frontend/app`、`frontend/public/stitch-pages` |
| 当前完成度 | 本文件、`功能完成度与待完善清单.md`、验收记录 |
| 设计目标与边界 | PRD、架构、Wiki、Agent、自进化设计文档 |
