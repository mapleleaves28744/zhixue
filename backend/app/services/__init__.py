"""Business service layer.

Service modules coordinate domain workflows, permissions, transactions,
repositories, agents, providers, and storage helpers. API routers should call
services instead of embedding business logic directly.
"""
